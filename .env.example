# Local/mock server defaults. Never put real credentials here.
BASE_URL=http://localhost:8000
PROCON_URL=http://localhost:8000
HOST=127.0.0.1
PORT=8000
MATCH_ID=local-demo
FIXTURE=fixtures/default.json

# Live tools read these names after sourcing .env.
# PROCON_USERNAME=team-id
# PROCON_PASSWORD=change-me

# Only needed when testing an authenticated local mock.
# API_TOKEN=local-test-token
