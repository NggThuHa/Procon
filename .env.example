# Local/mock server defaults. Never put real credentials here.
BASE_URL=http://localhost:8000
HOST=127.0.0.1
PORT=8000
MATCH_ID=local-demo
FIXTURE=fixtures/default.json

# Only needed when testing an authenticated local mock.
# API_TOKEN=local-test-token
